Ocean Professional Theme
- Variables and global UI primitives are defined in theme.css.
- Keep component styles minimal and rely on variables for consistency.
- Layout: header, sidebar, main, footer assembled via CSS grid in .app-shell.
